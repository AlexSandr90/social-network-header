import React from "react";

import ph from './photos.module.scss';

const Photos = () => {
    return (
        <div className={ph.photoContainer}>
            Photos
        </div>
    )
};

export default Photos;
